<%= form_with(model: men_shop_collection_category, local: true) do |form| %>
  <% if men_shop_collection_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(men_shop_collection_category.errors.count, "error") %> prohibited this men_shop_collection_category from being saved:</h2>

      <ul>
      <% men_shop_collection_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
